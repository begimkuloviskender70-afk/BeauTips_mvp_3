# ✅ Railway Deployment Checklist

Используйте этот чеклист перед деплоем на Railway для проверки готовности проекта.

## 📋 Подготовка проекта

### Файлы конфигурации
- [ ] `Procfile` создан и содержит правильную команду запуска
- [ ] `railway.json` настроен
- [ ] `nixpacks.toml` создан (опционально, но рекомендуется)
- [ ] `.gitignore` настроен (исключает `.env`, `__pycache__`, etc.)
- [ ] `.env.example` содержит все необходимые переменные

### Backend файлы
- [ ] `backend/requirements.txt` содержит все зависимости с версиями
- [ ] `backend/main.py` корректно настроен (без зависимости от frontend)
- [ ] `backend/database.py` использует `DATABASE_URL` из env
- [ ] `backend/models.py` содержит все модели
- [ ] `backend/auth.py` настроен для JWT
- [ ] `backend/schemas.py` содержит Pydantic схемы
- [ ] Все роутеры в `backend/routers/` на месте

### Тестирование локально (опционально)
- [ ] Запустили `python check_dependencies.py` - все зависимости установлены
- [ ] Локальный запуск работает: `cd backend && uvicorn main:app --reload`
- [ ] Swagger UI доступен: `http://localhost:8000/api/docs`

---

## 🔧 Railway Setup

### Создание проекта
- [ ] Зарегистрировались на [Railway.app](https://railway.app)
- [ ] Подключили GitHub аккаунт
- [ ] Создали новый проект

### База данных
- [ ] Создали PostgreSQL сервис в Railway
- [ ] Скопировали `DATABASE_URL` из Variables
- [ ] Проверили формат: `postgresql://...` (будет изменен в env vars)

### Репозиторий
- [ ] Создали Git репозиторий
- [ ] Сделали initial commit всех файлов
- [ ] Запушили на GitHub
- [ ] Подключили репозиторий к Railway

---

## 🔐 Переменные окружения

### Обязательные переменные в Railway

#### Database
- [ ] `DATABASE_URL` - **изменен на** `postgresql+asyncpg://...`
  ```
  Оригинал Railway:  postgresql://user:pass@host:port/db
  Нужно изменить на: postgresql+asyncpg://user:pass@host:port/db
  ```

#### JWT Authentication
- [ ] `SECRET_KEY` - сгенерирован (минимум 32 символа)
  ```bash
  # Генерация:
  python -c "import secrets; print(secrets.token_urlsafe(32))"
  ```
- [ ] `ALGORITHM=HS256`
- [ ] `ACCESS_TOKEN_EXPIRE_MINUTES=30`

#### Email (для верификации)
- [ ] `SMTP_HOST` - например: `smtp.gmail.com`
- [ ] `SMTP_PORT` - например: `587`
- [ ] `SMTP_USER` - ваш email
- [ ] `SMTP_PASSWORD` - App Password (не обычный пароль!)
- [ ] `FROM_EMAIL` - email отправителя
- [ ] `FROM_NAME=BeauTips`

#### Application
- [ ] `BASE_URL` - URL вашего Railway приложения
  ```
  https://your-app-name.up.railway.app
  ```
- [ ] `PORT` - оставьте пустым (Railway установит автоматически)

#### AI Services (если используется)
- [ ] `GOOGLE_API_KEY` - если используете Google Gemini

---

## 🚀 Деплой

### Автоматический через GitHub
- [ ] Railway подключен к репозиторию
- [ ] Сделан push в main/master ветку
- [ ] Build начался автоматически
- [ ] Build завершился успешно (проверьте логи)

### Или через Railway CLI
- [ ] Установлен `@railway/cli`
- [ ] Выполнен `railway login`
- [ ] Выполнен `railway init`
- [ ] Выполнен `railway up`

---

## ✅ Проверка после деплоя

### Основные проверки
- [ ] Приложение запущено (статус "Running" в Railway)
- [ ] Нет ошибок в логах Railway
- [ ] Приложение доступно по URL

### Endpoint тесты
- [ ] `GET /` - возвращает JSON со статусом
  ```bash
  curl https://your-app.railway.app/
  ```
- [ ] `GET /api/docs` - открывается Swagger UI
- [ ] `GET /api/test/schema` - работает
- [ ] `POST /api/auth/register` - endpoint работает (может вернуть 422)
- [ ] `POST /api/auth/login` - endpoint работает (может вернуть 422)

### Автоматическое тестирование
- [ ] Запустили `python test_api.py https://your-app.railway.app`
- [ ] Большинство тестов пройдено

### База данных
- [ ] Таблицы созданы автоматически (проверьте логи)
- [ ] Можно сделать тестовую регистрацию

---

## 🔍 Troubleshooting

### Если приложение не запускается
- [ ] Проверили логи в Railway Dashboard
- [ ] Все переменные окружения установлены
- [ ] `DATABASE_URL` в правильном формате (`postgresql+asyncpg://`)
- [ ] PostgreSQL сервис запущен

### Если есть CORS ошибки
- [ ] В `main.py` установлен `allow_origins=["*"]` для тестирования
- [ ] Frontend может подключиться к API

### Если база данных не подключается
- [ ] Проверили формат `DATABASE_URL`
- [ ] PostgreSQL сервис в статусе "Running"
- [ ] Правильные credentials в URL

---

## 📊 Post-Deployment

### Мониторинг
- [ ] Добавили проект в закладки
- [ ] Настроили алерты (опционально)
- [ ] Регулярно проверяете логи

### Документация
- [ ] URL приложения сохранен
- [ ] Документация обновлена с правильным URL
- [ ] Команда знает, где найти логи

### Безопасность
- [ ] Все секретные данные в Environment Variables
- [ ] `.env` не закоммичен в репозиторий
- [ ] SECRET_KEY достаточно сложный

---

## 🎯 Final Check

### Перед production release
- [ ] Все тесты пройдены
- [ ] CORS настроен на конкретные домены (не `["*"]`)
- [ ] Email верификация работает
- [ ] AI сервис работает (если используется)
- [ ] Производительность приемлемая
- [ ] Настроено автомасштабирование (если нужно)

---

## 📝 Примечания

Запишите здесь важную информацию о вашем деплое:

**Railway Project ID:** _________________

**Railway URL:** _________________

**Database Name:** _________________

**Дата деплоя:** _________________

**Версия:** _________________

---

✅ **Все готово!** Теперь ваш backend работает на Railway!

💡 **Совет:** Сохраните этот checklist и используйте для каждого деплоя или обновления.
