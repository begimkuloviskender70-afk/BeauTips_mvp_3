# 🚀 Инструкция по деплою BeauTips Backend на Railway

## 📋 Содержание
1. [Подготовка проекта](#подготовка-проекта)
2. [Регистрация на Railway](#регистрация-на-railway)
3. [Создание базы данных PostgreSQL](#создание-базы-данных-postgresql)
4. [Деплой Backend приложения](#деплой-backend-приложения)
5. [Настройка переменных окружения](#настройка-переменных-окружения)
6. [Проверка работоспособности](#проверка-работоспособности)
7. [Troubleshooting](#troubleshooting)

---

## 1️⃣ Подготовка проекта

### Структура проекта для Railway:
```
railway-backend/
├── backend/
│   ├── main.py
│   ├── database.py
│   ├── models.py
│   ├── auth.py
│   ├── schemas.py
│   ├── ai_service.py
│   ├── email_service.py
│   ├── exceptions.py
│   ├── requirements.txt
│   ├── routers/
│   │   ├── auth_router.py
│   │   └── history_router.py
│   └── migrations/
│       └── add_email_verification.sql
├── Procfile
├── railway.json
├── nixpacks.toml
├── .gitignore
├── .env.example
└── README_RAILWAY.md (этот файл)
```

### Проверка готовности:
- ✅ Все файлы backend скопированы
- ✅ requirements.txt содержит все зависимости
- ✅ .gitignore настроен
- ✅ Procfile и railway.json созданы

---

## 2️⃣ Регистрация на Railway

1. **Перейдите на** [railway.app](https://railway.app)
2. **Нажмите** "Start a New Project" или "Login with GitHub"
3. **Авторизуйтесь** через GitHub (рекомендуется)
4. **Подтвердите** email адрес

### Преимущества Railway:
- 💰 $5 бесплатно каждый месяц
- 🚀 Автоматический деплой из GitHub
- 🔄 Continuous Deployment
- 📊 Мониторинг и логи в реальном времени

---

## 3️⃣ Создание базы данных PostgreSQL

### Вариант 1: Встроенная БД Railway (рекомендуется)

1. **В Dashboard Railway** нажмите "+ New"
2. **Выберите** "Database" → "PostgreSQL"
3. **Дождитесь** создания (30-60 секунд)
4. **Откройте** PostgreSQL сервис
5. **Перейдите** на вкладку "Variables"
6. **Скопируйте** значение `DATABASE_URL`

⚠️ **ВАЖНО:** Railway предоставляет `DATABASE_URL` в формате:
```
postgresql://user:password@host:port/database
```

Но SQLAlchemy с asyncpg требует формат:
```
postgresql+asyncpg://user:password@host:port/database
```

**Решение:** Измените `postgresql://` на `postgresql+asyncpg://`

### Вариант 2: Внешняя БД

Можно использовать:
- [Neon](https://neon.tech) - бесплатный PostgreSQL
- [Supabase](https://supabase.com) - PostgreSQL с дополнительными фичами
- [ElephantSQL](https://www.elephantsql.com) - managed PostgreSQL

---

## 4️⃣ Деплой Backend приложения

### Способ 1: Деплой из GitHub (рекомендуется)

1. **Создайте репозиторий на GitHub:**
   ```bash
   cd railway-backend
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/your-username/beautips-backend.git
   git push -u origin main
   ```

2. **В Railway Dashboard:**
   - Нажмите "+ New" → "GitHub Repo"
   - Выберите репозиторий `beautips-backend`
   - Railway автоматически обнаружит Python проект

3. **Railway автоматически:**
   - Установит зависимости из `requirements.txt`
   - Запустит приложение согласно `Procfile`
   - Назначит публичный URL

### Способ 2: Деплой из локальных файлов

1. **Установите Railway CLI:**
   ```bash
   npm install -g @railway/cli
   ```

2. **Авторизуйтесь:**
   ```bash
   railway login
   ```

3. **Инициализируйте проект:**
   ```bash
   cd railway-backend
   railway init
   ```

4. **Деплой:**
   ```bash
   railway up
   ```

---

## 5️⃣ Настройка переменных окружения

### В Railway Dashboard:

1. **Откройте** ваш backend сервис
2. **Перейдите** на вкладку "Variables"
3. **Добавьте** следующие переменные:

#### Обязательные переменные:

```env
# Database
DATABASE_URL=postgresql+asyncpg://user:password@host:port/database

# JWT Authentication
SECRET_KEY=ваш-секретный-ключ-минимум-32-символа-используйте-генератор
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Email (для верификации)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-16-char-app-password
FROM_EMAIL=your-email@gmail.com
FROM_NAME=BeauTips

# Base URL (замените на ваш Railway URL)
BASE_URL=https://your-app-name.up.railway.app
```

#### Опциональные переменные:

```env
# Google Gemini AI (если используете AI функционал)
GOOGLE_API_KEY=your-google-api-key

# Port (Railway устанавливает автоматически)
PORT=8000
```

### 🔑 Генерация SECRET_KEY:

```bash
# Python
python -c "import secrets; print(secrets.token_urlsafe(32))"

# или OpenSSL
openssl rand -base64 32
```

### 📧 Настройка Gmail App Password:

1. Перейдите на [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords)
2. Создайте App Password для "Mail"
3. Скопируйте 16-символьный пароль
4. Используйте его в `SMTP_PASSWORD`

---

## 6️⃣ Проверка работоспособности

### После деплоя:

1. **Откройте URL** вашего приложения (предоставляется Railway)
   ```
   https://your-app-name.up.railway.app
   ```

2. **Проверьте API:**
   ```
   https://your-app-name.up.railway.app/api/docs
   ```
   Должна открыться Swagger UI документация

3. **Проверьте health endpoint:**
   ```bash
   curl https://your-app-name.up.railway.app/
   ```
   Ответ:
   ```json
   {
     "status": "running",
     "message": "BeauTips API is running",
     "version": "1.0.0",
     "docs": "/api/docs"
   }
   ```

4. **Проверьте логи:**
   - В Railway Dashboard перейдите на вкладку "Logs"
   - Убедитесь, что нет ошибок
   - Должны быть сообщения о запуске uvicorn

---

## 7️⃣ Troubleshooting

### Проблема: "Application failed to respond"

**Причина:** Приложение не запустилось или упало
**Решение:**
1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте формат `DATABASE_URL` (должен быть `postgresql+asyncpg://`)

### Проблема: "Module not found"

**Причина:** Не установлены зависимости
**Решение:**
1. Проверьте `requirements.txt`
2. Убедитесь, что все версии пакетов корректны
3. Пересоберите проект:
   ```bash
   railway up --detach
   ```

### Проблема: "Database connection failed"

**Причина:** Неверные credentials БД
**Решение:**
1. Проверьте `DATABASE_URL` в переменных окружения
2. Убедитесь в формате: `postgresql+asyncpg://`
3. Проверьте, что PostgreSQL сервис запущен

### Проблема: "CORS errors"

**Причина:** Frontend не может подключиться к API
**Решение:**
Текущие настройки разрешают все источники:
```python
allow_origins=["*"]
```
Для продакшена укажите конкретные домены:
```python
allow_origins=[
    "https://your-frontend-domain.com",
    "http://localhost:3000"  # для разработки
]
```

### Проблема: "Port already in use"

**Причина:** Railway автоматически назначает порт через переменную `$PORT`
**Решение:**
Приложение уже настроено использовать `$PORT`:
```python
uvicorn main:app --host 0.0.0.0 --port $PORT
```

---

## 🎯 Дополнительные рекомендации

### Безопасность:

1. **Никогда не коммитьте `.env` файлы**
2. **Используйте сильные SECRET_KEY** (минимум 32 символа)
3. **Для продакшена:** Ограничьте CORS конкретными доменами
4. **Регулярно обновляйте** зависимости

### Производительность:

1. **Настройте** Railway для автомасштабирования
2. **Используйте** connection pooling для БД
3. **Включите** кеширование для часто используемых запросов

### Мониторинг:

1. **Используйте** встроенные метрики Railway
2. **Настройте** алерты для критических ошибок
3. **Регулярно проверяйте** логи приложения

---

## 📞 Поддержка

- **Railway Docs:** [docs.railway.app](https://docs.railway.app)
- **Railway Discord:** [discord.gg/railway](https://discord.gg/railway)
- **FastAPI Docs:** [fastapi.tiangolo.com](https://fastapi.tiangolo.com)

---

## ✅ Checklist перед деплоем

- [ ] Все зависимости в `requirements.txt`
- [ ] `DATABASE_URL` в формате `postgresql+asyncpg://`
- [ ] `SECRET_KEY` сгенерирован (минимум 32 символа)
- [ ] Email настройки корректны (если используется)
- [ ] `BASE_URL` указывает на Railway URL
- [ ] `.gitignore` настроен (не коммитим `.env`)
- [ ] Проверена структура проекта
- [ ] PostgreSQL база данных создана на Railway
- [ ] Все переменные окружения добавлены в Railway

---

🎉 **Готово!** Ваш backend развернут на Railway и готов к использованию!
